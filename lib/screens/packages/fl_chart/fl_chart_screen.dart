import 'package:flutter/material.dart';

class FlChartScreen extends StatelessWidget {
  const FlChartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FL Charts'),
    );
  }
}
